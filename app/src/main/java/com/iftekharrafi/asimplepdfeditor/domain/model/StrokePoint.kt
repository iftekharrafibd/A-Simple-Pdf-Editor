package com.iftekharrafi.asimplepdfeditor.domain.model

/**
 * A simple, framework-agnostic point used to represent
 * drawing stroke paths in the domain layer.
 */
data class StrokePoint(val x: Float, val y: Float)
